services:
    glpi:
        image: glpi:10.0.17-alpine
        build:
            context: .
            dockerfile: Dockerfile
            target: base
        ports:
            - "8080:80"
        env_file: .env
        volumes:
            - app-files:/var/www/html/glpi/files
            - app-config:/var/www/html/glpi/config
            - app-plugins:/var/www/html/glpi/plugins
            - app-marketplace:/var/www/html/glpi/marketplace
    db:
        image: mysql:8.4.3
        environment:
            - "MYSQL_ROOT_PASSWORD=password"
        volumes:
            - db-data:/var/lib/mysql
volumes:
    db-data:
    app-files:
    app-config:
    app-plugins:
    app-marketplace:
